# ═══════════════════════════════════════════════════════════
# GTM Engine — Environment Variables
# Copy to .env and fill in your keys
# ═══════════════════════════════════════════════════════════

# ── Shared API Keys (same across all clients) ────────────
TRIGIFY_API_KEY=
VAYNE_API_KEY=
ANYMAILFINDER_API_KEY=
ANTHROPIC_API_KEY=

# ── Per-client keys are in config/clients/<slug>.json ────
# Each client has its own:
#   - leadgenius.api_key
#   - leadgenius.admin_key
#   - neon.dsn
#   - notion.api_key (optional, if separate workspace)
